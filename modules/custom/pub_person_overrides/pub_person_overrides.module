<?php
/**
 * @file
 * Code for the pub_person_overrides feature.
 */

include_once 'pub_person_overrides.features.inc';

/**
 * Implements hook_preprocess_HOOK().
 */
function pub_person_overrides_preprocess_page(&$vars) {
  // Load node, hide title on the frontend
  $node = menu_get_object();
  if ($node && $node->type == 'person') {
    $vars['title'] = '';
  }
}

